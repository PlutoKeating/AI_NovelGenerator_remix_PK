"""Service layer — business logic that sits between API routers and the
underlying pipeline / file system / config store.

Importing this package must NOT have side-effects beyond what the individual
service modules require.
"""
